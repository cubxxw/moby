interface:
  display_name: "Moby"
  short_description: "Repo-specific patterns and workflows for moby"
  default_prompt: "Use the moby repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true